# stalkerMap WIP
-Port Scanner <br />
-Directory enumerator <br />
-Organizes retrieved data on files <br />

# Be aware!
(Only DNS related functionality is working atm) <br />
-This tool uses on occasion the command "dig" for dns querries, so please have it up to date to avoid errors <br />
-Type on the terminal: "node ." or "node stalkermap.js" inside the project directory into execute it <br />
-All info gathered by the tool will be store on "./data/{targetName}"  <br />
(WIP - to see the stored data take a look on "./data/appData/{targetName}")